## 1. 隐藏注册 UI 元素

- [x] 1.1 在 `public/index.html` 中，给 `#authTabRegister` 按钮添加 `style="display:none"` 属性
- [x] 1.2 在 `openAuthModal` 函数中，将 `tab` 参数为 `'register'` 时强制改为 `'login'`

## 2. 验证

- [x] 2.1 打开登录弹层，确认只显示登录标签，注册标签不可见
- [x] 2.2 确认登录功能正常可用
