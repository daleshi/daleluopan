## 1. 前端路由判断逻辑

- [x] 1.1 在 `DOMContentLoaded` 入口处添加路径检测：读取 `window.location.pathname`，判断是否为 `/admin`
- [x] 1.2 非 `/admin` 路径时，隐藏顶部导航登录按钮（`#loginBtn` 或对应元素）
- [x] 1.3 `/admin` 路径时，隐藏主应用内容区（导航栏、数据面板），展示登录视图

## 2. /admin 登录视图

- [x] 2.1 在 HTML 中新增登录卡片容器 `#adminLoginView`（默认隐藏），包含居中样式的登录表单
- [x] 2.2 复用现有登录表单的 HTML 结构（用户名/密码输入框、登录按钮、错误提示区）
- [x] 2.3 添加 CSS：`#adminLoginView` 全屏居中布局，与主题系统保持一致（支持深色/浅色）

## 3. 已登录用户访问 /admin 的跳转

- [x] 3.1 `/admin` 路径初始化时，调用 `/api/auth/me` 检查登录状态
- [x] 3.2 若已登录，立即执行 `window.location.href = '/'` 跳转回根路径

## 4. 登录成功后跳转

- [x] 4.1 在 `/admin` 登录视图的登录成功回调中，执行 `window.location.href = '/'`
- [x] 4.2 登录失败时，在卡片内显示错误信息，不跳转

## 5. 未登录触发认证操作时跳转 /admin

- [x] 5.1 找到所有调用 `openAuthModal()` 的位置（需认证操作的触发点）
- [x] 5.2 将这些调用替换为 `window.location.href = '/admin'`（根路径下无登录弹窗）

## 6. 验证

- [ ] 6.1 访问根路径 `/`，确认顶部无登录按钮
- [ ] 6.2 访问 `/admin`（未登录），确认显示登录卡片，主应用内容隐藏
- [ ] 6.3 在 `/admin` 登录成功，确认跳转至 `/` 并正常显示数据
- [ ] 6.4 已登录状态访问 `/admin`，确认自动跳转回 `/`
- [ ] 6.5 未登录时触发需认证操作，确认跳转至 `/admin` 而非弹出模态框
